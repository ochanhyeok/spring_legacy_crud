<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.crud_ex.mapper.MemberMapper">
    <!-- 회원 조회 (로그인용) -->
    <select id="findByUserId" resultType="Member">
        SELECT * FROM member
        WHERE user_id = #{userId}
    </select>

    <!-- 회원 가입 -->
    <insert id="save">
        INSERT INTO member(user_id, user_pw, user_name, email)
        VALUES (#{userId}, #{userPw}, #{userName}, #{email})
    </insert>

    <!-- 회원 정보 수정 -->
    <update id="update">
        UPDATE member
        SET user_pw = #{userPw},
            user_name = #{userName},
            email = #{email}
        WHERE uesr_id = #{userId}
    </update>

    <!-- 회원 탈퇴 -->
    <delete id="delete">
        DELETE FROM member
        WHERE user_id = #{userId}
    </delete>

    <!-- 아이디 중복 체크 -->
    <select id="existsByUserId" resultType="int">
        SELECT COUNT(*) FROM member
        WHERE user_id = #{userId}
    </select>


</mapper>