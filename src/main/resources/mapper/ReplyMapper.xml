<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.crud_ex.mapper.ReplyMapper">
    <!-- 특정 게시글의 댓글 목록 조회 -->
    <select id="findByBoardId" resultType="Reply">
        SELECT * FROM reply
        WHERE board_id = #{boardId}
        ORDER BY reply_id DESC
    </select>

    <!-- 내가 작성한 댓글 조회 -->
    <select id="findByReplyer" resultType="Reply">
        SELECT * FROM reply
        WHERE replyer = #{replyer}
        ORDER BY reply_id DESC
    </select>

    <!-- 댓글 한 건조회 -->
    <select id="findOne" resultType="Reply">
        SELECT * FROM reply
        WHERE reply_id = #{replyId}
    </select>

    <!-- 등록 -->
    <insert id="save" useGeneratedKeys="true" keyProperty="replyId">
        INSERT INTO reply(board_id, content, replyer)
        VALUES (#{boardId}, #{content}, #{replyer})
    </insert>

    <!-- 수정 -->
    <update id="update">
        UPDATE reply
        SET content = #{content}
        WHERE reply_id = #{replyId}
    </update>

    <!-- 삭제 -->
    <delete id="delete">
        DELETE FROM reply
        WHERE reply_id = #{replyId}
    </delete>

    <!-- 총 개수 -->
    <select id="getCountByBoardId" resultType="int">
        SELECT COUNT(*) FROM reply
        WHERE board_id = #{boardId}
    </select>


</mapper>